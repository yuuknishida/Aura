[project]
name = "server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "gputil>=1.4.0",
    "psutil>=7.2.2",
]

[tool.fastapi]
entrypoint = "server.app:app"
