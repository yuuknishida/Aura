export default class Metrics {
    constructor(label, value, unit, peak, color) {
        this.label = label;
        this.value = value;
        this.unit = unit;
        this.peak = peak;
        this.color = color;
    }
}